{
  "name": "@usefulthings/uikit",
  "version": "0.1.1",
  "description": "",
  "license": "UNLICENSED",
  "author": "Stephen Chen",
  "main": "dist/index.js",
  "module": "dist/index.module.js",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "build": "rollup --config",
    "publish": "npm run build && npm publish --access public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "rollup": "^2.50.6",
    "rollup-plugin-delete": "^2.0.0"
  },
  "peerDependencies": {
    "@emotion/css": "^11.1.3"
  }
}
